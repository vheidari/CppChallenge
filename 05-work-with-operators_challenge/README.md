## What is challenge :

	-- work with operators in c++
		-- Operators `+` `-` `*` `/` `%`
		-- Operators `++` and `--`  postfix and prefix
		-- Operators `==` and `!=` and `<` and `>` and `<=` and `>=` 
		-- Operators `!` ane `&&` ane `||`